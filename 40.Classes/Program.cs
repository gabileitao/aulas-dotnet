using System;
using System.Collections.Generic;

namespace _40.Classes {
    class Program {
        static void Main(string[] args) {
            //Toda classe sempre tem que estar dentro de um arquivo,
            //mas é preferível que seja apenas uma por arquivo.

            //Existem classes e objetos. Este é sempre criado a partir de uma classe.
            //Classe e tipo (de variáveis) são sinônimos. 
            //- Tipo struct (int, float, string, char, bool) são os primitivos [eles são passados por valor]. +byVal(value)
            //- Tipo class são [passados por referência] mais abrangentes. +byRef(reference)

            //Qual a diferença de classe e obj.? Classes são moldes e objs. são instâncias criadas nesses moldes. Uma classe
            //é como uma plante de um prédio e os objs. são os prédios criados com base nessa planta. Instância e objeto são
            //sinônimos. 
            //Quando você instancia uma classe, você gera um objeto.

            //Dentro de uma classe podemos criar:
            // atributos/atribute = campo/field, property/propriedade (variáveis)
            // funções/function = método/method

            //O bloco principal é o NAMESPACE dentro dele você pode apenas criar CLASS, no qual podem ser criados apenas
            //ATRIBUTOS e FUNÇÕES, onde você insere o ALGORITMO/COMANDOS.

            //Atributos estão para CLASS assim como VARIÁVEIS estão para FUNÇÕES.
        }
    }
}
